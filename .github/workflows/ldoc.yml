on:
    pull_request:
      branches:
      - ldoc-test
    push:
      branches:
      - ldoc-test

jobs:
  ldoc:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Generate docs with LDoc
        uses: lunarmodules/ldoc@v0